# 代码规范

- 命名：
  - 函数使用蛇形命名（如 `user_register`）。
  - 宏常量使用全大写（如 `USERNAME_MAX_LEN`）。
- 注释：关键结构体与核心函数必须添加中文注释。
- 文件组织：模块头文件放 `include/`，实现放 `src/`。
- 输入校验：所有关键输入（菜单、ID、用户名、密码）均需校验。
- 日志：关键操作必须调用 `log_action`。
